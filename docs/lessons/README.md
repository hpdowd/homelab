# Lessons (Incident Log)

Post-mortems filed after anything that caused downtime, data risk, or significant wasted time. Organised by domain:

- `infra/`, Proxmox host, hardware, networking below k3s
- `k8s/`, cluster-level issues (pods, controllers, storage)
- `networking/`, DNS, Traefik, cloudflared, tunnels
- `storage/`, Longhorn, ZFS, PVC issues
- `backup/`, restic, B2, restore failures

Use `TEMPLATE.md` for new entries. Each entry covers: date · context · symptoms · investigation (including dead ends) · root cause · fix · verification · prevention.

## Current entries

| File | Domain | Summary |
|---|---|---|
| infra/e1000e-nic-hang.md | infra | Intel onboard NIC TX ring stall — host unreachable, required hard reboot |
| infra/nextcloud-lxc-firewall-port11000.md | infra | Proxmox LXC firewall silently blocked port 11000 (AIO frontend) |
| infra/wireguard-lxc-dstate-freeze.md | infra | `pct enter` into WireGuard LXC while VPN active → D-state freeze, host reboot required |
| k8s/grafana-pvc-corruption.md | k8s | Reused Grafana PVC with corrupt SQLite across reinstalls → CrashLoopBackOff |
| k8s/grafana-monitoring-sync-cascade.md | k8s | Six-bug cascade: orphaned ArgoCD app → kiwix syncOptions → PrometheusRule vs VMRule → plugin 404 → datasource isDefault collision → Grafana OOMKill |
| k8s/k3s-control-plane-false-positives.md | k8s | ~12 false-positive alert emails/night — chart's control-plane rules (scheduler/CM/etcd/proxy Down) assume kubeadm; k3s runs them in-process. Verified alive before silencing |
| k8s/argocd-comparisonerror-silent-values.md | k8s | Bad inline Helm values (indent / duplicate key / typo'd key) → silent ArgoCD ComparisonError or last-wins; live workloads keep running old spec, no surface symptom |
| k8s/alertmanager-null-route-typo.md | k8s | `reciever` typo → drop route inherits `email`, so Watchdog leaked to inbox; drop route ordered after critical; `InforInhibitor` matcher matched nothing. Silent — config loaded fine |
| k8s/collabora-slow-load-wordbook.md | k8s | Collabora cold opens ~55 s — hunspell en_GB.dic uploaded as personal wordbook; DictionaryNeo quadratic load per kit. Two-day detour fixing a coincident jail-copy/AppArmor pathology first (raw log alongside) |
| networking/proxmox-502-selfsigned-tunnel.md | networking | Cloudflare tunnel returned 502 for Proxmox — self-signed cert on :8006 |
| networking/vodafone-hub-ghost-portforward.md | networking | Vodafone Hub silently wiped port-forward rules after DHCP change |
| k8s/argocd-selfheal-backup-race.md | k8s | selfHeal reverted the Gitea backup's scale-to-0 within seconds — every "consistent" SQLite backup actually ran against a live Gitea. Fixed with ignoreDifferences on replicas |
| storage/zfs-snapshot-retention-noop.md | storage | Nightly ZFS prune failed silently since setup (`zfs destroy` multi-arg) — and its selection logic would have deleted the wrong snapshots if it had worked |
| networking/certmanager-dns01-split-horizon.md | networking | First wildcard issuance stuck twice: Technitium answers the local zone's NS as `technitium.` (poisons the DNS-01 self-check), then the Vodafone hub turned out to drop *all* outbound port 53 — self-check moved to DoH. Also exposed Collabora's silently broken DNS hairpin |
| k8s/act-runner-dind-mtu-oom.md | k8s | Gitea Actions bring-up: DinD bridge MTU 1500 vs pod 1450 black-holed job internet TLS (`curl (35)` reset after ~2.5min; checkout worked); plus act_runner Go-GC OOMKilled at 192 & 384Mi until `GOMEMLIMIT` + 768Mi |
| k8s/act-runner-no-docker-daemon.md | k8s | act_runner job steps can't `docker build` — the dind sidecar gives the *runner* a daemon, not the job container (no `docker.sock`, `DOCKER_HOST` not propagated). Portfolio's image build moved to GitHub-hosted runners |
| networking/proxmox-401-secure-cookie-plain-http.md | networking | Proxmox login 401'd through Traefik (and public 404'd): PVE's auth cookie is `Secure`, so a plain-HTTP browser drops it → 401 right after login; route was web-only so HSTS/split-horizon hit websecure → 404. Fixed with a second `websecure` IngressRoute (a `tls` block is required to serve websecure but makes the router TLS-only, so it must be two routes) |
| k8s/worker-reboot-alert-storm.md | k8s | A worker reboot fired ~20 `PodCrashLooping` alerts ~20 min late, all for already-healthy pods (Longhorn reattach crashloops + the alerter on the same worker, batch-flushed on recovery; triage by the *absence* of `PodOOMKilled`). Delayed second act: the reboot's salvage/rebuild work bloated the control-node `longhorn-manager` ~150MiB hours later, tripping `NodeMemoryLowControl` — fixed by bouncing the manager |
| k8s/nextcloud-cron-multiattach-rwo.md | k8s | `nextcloud-cron` failed ~half its runs with `KubeJobFailed` — RWO `nextcloud-data` attaches to the main pod's node, but the CronJob had no affinity so off-node runs hit `FailedAttachVolume` → `DeadlineExceeded`. HOMELAB.md already documented "podAffinity pins to worker"; the manifest had drifted. Fixed with podAffinity on `app: nextcloud`. Audited the backup CronJobs (nextcloud/gitea/immich) — all already carry the affinity; the 5-min cron was the lone job that missed the pattern |
| k8s/netpol-fresh-pod-race.md | k8s | ADR 011's default-deny-ingress netpol broke `nextcloud-cron` — postgres "connection refused" while `occ status` from the live pod reached the same DB fine. kube-router adds a fresh pod's IP to its namespace source-set seconds *after* start (probed: blocked t=0, open t=14s), so short-lived cron/backup pods connecting to a same-ns DB in their first seconds lose the race and are REJECTed. Reverted the immich/nextcloud netpols, kept the 5 race-safe ones + all securityContext; re-adding needs a `pg_isready` wait-guard |
| k8s/dowd-ie-cert-wrong-namespace.md | k8s | `dowd-ie` Certificate copy-pasted the `traefik`-namespace placement from `henrydowd-dev-cert.yaml`, but that reason (TLSStore cross-ref) didn't apply — it's consumed by a plain `Ingress`, which only resolves `tls.secretName` same-namespace. cert-manager reported healthy the whole time; Traefik silently served the wrong (henrydowd.dev) cert for `secure.dowd.ie` and logged the exact reason on every resync. Fixed by moving the Certificate to `file-parser` ns |
| k8s/file-parser-oom-large-pdf.md | k8s | A 200+ page PDF upload OOMKilled the single-replica file-parser pod (1Gi limit) — 502, then "couldn't connect to server", then the alert email. Parser built the whole document in memory before returning anything, so cost scaled with page count, not file size. Mitigated same-day with a 1Gi→3Gi bump, then actually fixed by streaming the parse page-by-page (peak ~112Mi even on 200+ pages) and dropping the limit back down. See ADR 013 for the follow-on engine swap |
| storage/longhorn-autosalvage-blocked-diskpressure.md | storage | All 11 Longhorn volumes faulted at once and stayed down 16h overnight. A hypervisor-initiated shutdown stopped `open-iscsi` before containerd finished, dropping 12 iSCSI sessions under live volumes. The reboot is the trigger, not the story: `auto-salvage` was `true` and fired 4,367 times, every one "Bringing up 0 replicas", because the data disk sat at `Schedulable: False`/`DiskPressure` — 351 GiB of *provisioned* volumes against a 343.8 GiB limit, caused by a 30% (147 GiB) reservation applied to a dedicated data disk. Salvaged by clearing `failedAt`; all filesystems mounted with no journal replay, all three postgres did clean WAL redo. Over-provisioning still unfixed, so it recurs |
| backup/restic-retention-never-pruned.md | backup | restic retention had never deleted a single snapshot since the CronJobs were created — 55 gitea / 90 immich / 104 nextcloud against a 7d/4w/3m policy. `forget` defaults to `--group-by host,paths`, and a pod's hostname is its pod name, unique per run, so every snapshot became a retention group of one and `--keep-daily 7` dutifully kept it. Prune was never broken and the policy was never wrong; the grouping made it a no-op, silently and in the safe direction. Found sideways while verifying two stale `BackupJobFailed` alerts. Also cleared a six-week-old foreign-hostname lock in the nextcloud repo (restic can't judge such locks stale, so it blocked prune there independently) and noted the `\|\| true` that hid it. Fixed with `--group-by tags`; dry-run drops gitea 55 → 12 |
| k8s/homepage-readonly-config-erofs.md | k8s | homepage (v1.13.2) crashlooped intermittently *after* it was verified healthy — `EROFS: copyfile skeleton/proxmox.yaml -> /app/config/proxmox.yaml`. The config ConfigMap was mounted read-only over the whole `/app/config`, but homepage *lazily* copies skeleton/provider files into that dir at runtime (not all at startup — why it booted clean twice first). Hand-listing every file it "looks for" missed `proxmox.yaml` and is fragile anyway. Fixed by making `/app/config` a writable emptyDir seeded by an initContainer, letting homepage create its own files. The write path, not the file list, was the problem |
| k8s/homepage-prerender-erofs.md | k8s | The dashboard served the image's built-in demo page ("My First Service") on every hostname for nine days while pod, probes and ArgoCD all read healthy. Homepage prerenders `/app/.next/server/pages/en.html` at image build time and rewrites it via `/api/revalidate` — a write the July fix never made possible, so it failed EROFS once at boot, warned only in `/app/config/logs/homepage.log`, and never retried. Client-side hydration patched the service list back in, which hid it; title, `headerStyle` and `layout` stayed wrong. Fixed with a third writable emptyDir seeded from the image + a one-shot `startupProbe` on `/api/revalidate`. `%{http_code}` 200 is not a smoke test for a page |
| k8s/kiwix-cluster-cache-oom.md | k8s | `PodOOMKilled` on kiwix that looked self-resolved — pod Running/Ready, `RESTARTS 1`. Actually a ~24h sawtooth: the "recovered" pod was already at 91% of its limit 18 min after restarting. Node was never starved (3.8–4.1 GiB free throughout) and the 512Mi limit had held 25x headroom for a month. Splitting the cgroup counters ruled out the intuitive mmap/page-cache answer (`mapped_file` ~6 MiB throughout; `cache` being *squeezed* 260→50 while `rss` climbed 17→452). libzim caches decompressed clusters bounded by count **per ZIM** (`ZIM_CLUSTERCACHE`=16), so 12 ZIMs ≈ 450 MiB — latently wrong for months, exposed only when a bot crawl started walking the public wiki at 1–3 req/s. Fixed by bounding the cache, not raising the limit (which only moves the OOM out a day) |
