apiVersion: discovery.k8s.io/v1
kind: EndpointSlice
metadata:
  name: amp-external
  namespace: amp
  labels:
    kubernetes.io/service-name: amp-external
addressType: IPv4
endpoints:
  - addresses:
      - 192.168.1.15
    conditions:
      ready: true
ports:
  - port: 8080
    protocol: TCP
