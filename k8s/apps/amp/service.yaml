# Deliberately no selector — the backend is the hand-written EndpointSlice
# in endpoints.yaml (LXC 102), not pods.
apiVersion: v1
kind: Service
metadata:
  name: amp-external
  namespace: amp
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
