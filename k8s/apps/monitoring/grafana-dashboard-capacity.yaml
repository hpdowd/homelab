# A custom "Capacity / RAM headroom" dashboard, baked into a ConfigMap so the
# Grafana sidecar picks it up automatically (the grafana_dashboard label is
# what makes that happen — configured in victoria-metrics.yaml).
#
# This one answers a single question: how much real memory is free on each
# node, and which pods are eating it? That's the gating factor for adding
# Immich, so it gets its own board.
#
# Useful community dashboards to import in the Grafana UI on first run:
#   - 1860   Node Exporter Full        (host CPU/RAM/disk/net)
#   - 15757  Kubernetes / Views / Global
#   - 13332  kube-state-metrics v2     (pod/deploy/job health)
#   - 17626  Longhorn                  (needs Longhorn's metrics endpoint scraped)
apiVersion: v1
kind: ConfigMap
metadata:
  name: grafana-dashboard-capacity
  namespace: monitoring
  labels:
    grafana_dashboard: "1"
data:
  homelab-capacity.json: |
    {
      "title": "Homelab — Capacity & RAM headroom",
      "uid": "homelab-capacity",
      "tags": ["homelab", "capacity"],
      "timezone": "browser",
      "schemaVersion": 39,
      "refresh": "30s",
      "time": { "from": "now-6h", "to": "now" },
      "panels": [
        {
          "type": "timeseries",
          "title": "Available memory per node (gating Immich)",
          "gridPos": { "h": 9, "w": 24, "x": 0, "y": 0 },
          "fieldConfig": { "defaults": { "unit": "bytes" } },
          "targets": [
            {
              "expr": "node_memory_MemAvailable_bytes",
              "legendFormat": "{{instance}} available"
            }
          ]
        },
        {
          "type": "timeseries",
          "title": "Swap used per node (you already touch swap)",
          "gridPos": { "h": 8, "w": 12, "x": 0, "y": 9 },
          "fieldConfig": { "defaults": { "unit": "bytes" } },
          "targets": [
            {
              "expr": "node_memory_SwapTotal_bytes - node_memory_SwapFree_bytes",
              "legendFormat": "{{instance}} swap used"
            }
          ]
        },
        {
          "type": "bargauge",
          "title": "Top pods by memory (where the RAM goes)",
          "gridPos": { "h": 8, "w": 12, "x": 12, "y": 9 },
          "fieldConfig": { "defaults": { "unit": "bytes" } },
          "options": { "displayMode": "gradient", "orientation": "horizontal" },
          "targets": [
            {
              "expr": "topk(12, sum by (namespace, pod) (container_memory_working_set_bytes{container!=\"\", pod!=\"\"}))",
              "legendFormat": "{{namespace}}/{{pod}}",
              "instant": true
            }
          ]
        },
        {
          "type": "stat",
          "title": "Cluster memory committed vs allocatable",
          "gridPos": { "h": 7, "w": 24, "x": 0, "y": 17 },
          "fieldConfig": { "defaults": { "unit": "percent" } },
          "targets": [
            {
              "expr": "100 * sum(container_memory_working_set_bytes{container!=\"\"}) / sum(kube_node_status_allocatable{resource=\"memory\"})",
              "legendFormat": "cluster memory used %"
            }
          ]
        }
      ]
    }
